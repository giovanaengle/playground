architecture: yolo
data: .configs/ingest.yaml
framework: ultralytics
path: .pipeline/vehicle/models/train/weights/best.pt
output: .pipeline/vehicle/models
params: .configs/ultralytics.yaml
task: detect
top5: false # applied only for classification results
weights:
